
const LearnButton = ({className}) => {
    return (
        <button className={`bg-primary text-white font-primary font-semibold text-base ${className}`}>
            Learn More
        </button>
    );
};

export default LearnButton;